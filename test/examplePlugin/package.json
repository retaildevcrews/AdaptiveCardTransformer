{
	"name": "example-plugin",
	"version": "1.0.0",
	"description": "Example plugin to be loaded into pluginLoader",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC"
}
